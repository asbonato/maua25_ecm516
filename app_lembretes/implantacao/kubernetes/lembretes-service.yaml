apiVersion: v1
kind: Service
metadata:
  name: lembretes-service
spec:
  type: NodePort
  selector:
    app: lembretes
  ports:
    - name: lembretes
      protocol: TCP
      #o nó recebe requisições nesta porta
      port: 4000
      #e direciona para esta porta do Pod
      targetPort: 4000